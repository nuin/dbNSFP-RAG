# Data files (too large for git)
data/
*.index
*.pkl

# ClickHouse data (old)
store/
metadata/
preprocessed_configs/
status
uuid

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
*.egg-info/

# uv
.python-version
uv.lock

# Model files (too large for git)
models/acmg-classifier/model/
models/acmg-classifier/adapters/
models/acmg-classifier/data/
*.safetensors
*.bin
*.gguf

# macOS
.DS_Store

# Runtime artifacts
logs/

# Local Claude settings (user-specific permissions)
.claude/settings.local.json

# Tabix index files dropped at repo root by remote-tabix calls
*.tbi
*.csi

# SpliceAI / large reference files (kept under ~/data/, not in repo)
hg38.fa
hg38.fa.fai
hg38.fa.gz

# dbt POC — venv and build artifacts (regenerable)
.venv-dbt/
lake_poc/lake.duckdb
lake_poc/target/
lake_poc/dbt_packages/
lake_poc/logs/
lake_poc/package-lock.yml

# Large regenerable data dumps (never commit — exceed GitHub 100MB)
*viz.db
*all_annotations.tsv.gz
cp_new_bundle/outputs/_raw/viz.db
cp_new_bundle/outputs/_raw/all_annotations.tsv.gz
